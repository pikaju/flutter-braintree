name: flutter_braintree_example
description: Demonstrates how to use the flutter_braintree plugin.
version: 0.6.0
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_braintree:
    path: ../

flutter:
  uses-material-design: true
